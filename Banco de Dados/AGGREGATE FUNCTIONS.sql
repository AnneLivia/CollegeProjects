
SELECT MAX(age) AS MAXAGE FROM clientes;
## AVG(columnname) AS something FROM clientes;
## MIN(columnname) AS something FROM clientes;
## COUNT(*) -> how many rows there are in a table containg value or null values
## COUNT(columnname) -> how many rows there are in a specific colunm ignoring null values
## SUM(Columnname) AS something FROM clientes; -> sum the column values