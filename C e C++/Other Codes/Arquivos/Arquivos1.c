#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    FILE *pFile;
    pFile = fopen("numeros.txt","r"); // r - read.

    // Deve se fazer essa verificação se o arquivo existe ou não, pois se não ele pode retornar lixo

    if(pFile == NULL)  // verifica se o ponteiro está apontando pro nada.
    {
        printf("Arquivo nao pode ser aberto pois ele nao existe.\n");
        return 0;
    }

    int x,y,z;
    fscanf(pFile,"%d %d %d",&x,&y,&z);

    printf("%d %d %d",x,y,z);
    fclose(pFile);
    return 0;
}
*/
