#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    FILE *pFile;
    pFile = fopen("texto.txt","a");  // a de atend. me esqueci o significado

    if(pFile == NULL)
    {
        printf("Arquivo nao existe\n");   // Verificar se arquivo existe
        exit(0);
    }

   /* char frase[100];

    while(fgets(frase,100,pFile)!= NULL)     // Forma de le as frases digitadas no arquivo testo. fgets(nomedovetor, tamanhodovetor, ponteiroArquivo)
    {
        printf("%s",frase);
    }
    */

  /*  fprintf(pFile,"\nPrimeira linha\n");   // Função para colocar no arquivo algum texto fprintf(ponteiroArquivo,"frase");

    char frase[] = {"Segunda linha\n"};    // Função para colocar no arquivo algum texto de vetor fputs(variavel,ponteiro);
    fputs(frase,pFile);

    char caractere = '3';                   // Função para colocar no arquivo algum caractere fputc(variavel,ponteiro);
    fputc(caractere,pFile);

    fclose(pFile);
    return 0;
}
*/
