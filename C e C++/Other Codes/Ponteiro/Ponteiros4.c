#include <stdio.h>
#include <stdlib.h>


// Ponteiro que aponta para um vetor.
 // Manipulação dos valores do vetor.

/*
int main()
{
    int vetor[3] = {1,2,3};

    // Dessa forma que criamos um ponteiro que aponta para um vetor
    int *ponteiro = vetor;

    /*ponteiro = &vetor[1];
    printf("%p\n", ponteiro);

    ponteiro = &vetor[2];
    printf("%p\n", ponteiro);
    */

    /*  ++ponteiro; //Pulou um endereço de memoria, agora irá mostrar o valor do vetor[2].
        ++ponteiro; // Aqui pulou outra vez e irá mostrar o valor do vetor[3].
        printf("Valores vetores: %i\n",*ponteiro);
        //Alterando valor de algum indice do vetor utilizando ponteiro.
    */
/*
    printf("Valor do vetor 2 antes de alterar: %d\n",vetor[1]);

    *(ponteiro + 1) = 15;

    printf("Valor do vetor 2 depois de alterar: %d\n",vetor[1]);
    return 0;
}
*/
