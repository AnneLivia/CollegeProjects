#include <stdio.h>
#include <stdlib.h>


int main()
{

    // Função que chama outra função
   /* float somanumeros(float a, float b);
    float valorabsoluto(float a);
    float a,b;
    printf("Digite um valor: ");
    scanf("%f",&a);
    printf("Digite outro valor: ");
    scanf("%f",&b);

    printf("O Valor absoluto de %.2f + %.2f e %.2f",a,b,somanumeros(a,b));
    return 0;
}

float somanumeros(float a, float b)
{
    float valorabsoluto(float a);
    if(a < 0)
    {
        a = valorabsoluto(a);
    }

    if(b < 0)
    {
        b = valorabsoluto(b);
    }
    return a + b;
}

float valorabsoluto(float a)
{
    return a = a*(-1);
}*/

// Função recursiva


return 0;
}
