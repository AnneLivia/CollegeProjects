#include <stdio.h>
#include <stdlib.h>
/*
int main()
{
    struct lista
    {
        int valor;
        struct lista *pProximo;  // Ponteiro chamado proxímo que aponta para um membro de uma outra estrutura lista.
    };

    struct lista membro1,membro2,membro3;
    struct lista *gancho = &membro1;

    membro1.valor = 10;
    membro2.valor = 20;
    membro3.valor = 30;

    membro1.pProximo = &membro2;
    membro2.pProximo = &membro3;
    membro3.pProximo = (struct lista *)0;

    while(gancho != (struct lista *)0)
    {
        printf("%i\n",gancho->valor);
        gancho = gancho->pProximo;
    }
    return 0;
}
*/
